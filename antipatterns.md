# Антипаттерны генерации макета

Правила для Кодекса, который выдаёт экраны в Figma. Каждый пункт — из разбора
файла `lKdFEoJiOwWMaUpjTHBl6N` (Invoice Maker), с числом, которое он породил.

Полный разбор: <https://gogi213.github.io/invoice-consistency/>

---

## 1. Ширина и масштаб

### ❌ Генерировать не в той ширине, в которой объявлена система

Система объявлена на **375 pt**, Кодекс отдавал **430 pt**. Каждую партию
сжимали вручную.

**Цена:** 4140 дробных значений, 91,9 % из них — след одного умножения на 0,872.
Радиус 20 стал 17,44, кегль 12 → 10,47, отступ 10 → 8,72.

✅ Генерировать ровно в ширине мастера. Нужна другая ширина — менять её
в генерации, а не после.

### ❌ Масштабировать готовые экраны

Инструмент Scale меняет **всё**, включая кегли. Ресайз фрейма кегли не трогает —
именно по этому и видно, чем сжимали.

**Цена:** за полтора часа 22 июля дробных значений стало 229 → 3303.

✅ Нужен другой размер — перегенерировать. Никогда не жать существующее.

### ❌ Оставлять дробные значения

Ни одно свойство не должно быть дробным: ни радиус, ни кегль, ни отступ,
ни обводка.

**Цена в файле:** 508 радиусов, 2038 кеглей, 1439 отступов, 155 обводок.

✅ Все значения — целые числа из объявленных шкал.

---

## 2. Имена слоёв

### ❌ Переносить теги разметки в имена

Найдено: `Container` ×545, `Text` ×329, `Paragraph` ×103, **`Italic Text` ×191**,
**`Bold Text` ×32**, `Heading 1` ×46, `Article`, `Section`, `Header`, `Footer`.

Плюс **146 имён с CSS-свойством**: `Container:margin`.

**Цена:** кнопка «назад» названа `Container` — её не найти ни поиском, ни
скриптом. Экран ошибки собран из экрана успеха, и слои так и остались
`Success icon`, `Success mark`, `Success title`, `Success helper` — на экране
с текстом «Invoice wasn't sent».

✅ Имя описывает роль в интерфейсе: `Back button`, `Invoice row / Amount`,
`Status / Unpaid`. Не `<em>`, не `div`, не CSS-свойство.

### ❌ Называть один и тот же элемент по-разному

Статус-бар назван **шестнадцатью способами** на 93 экранах. Регистр расходится
внутри одного набора: `Status / signal` и `Status / Signal`.

**Цена:** собрать в компонент нельзя, пока нет одного словаря.

✅ Один элемент — одно имя, один регистр.

---

## 3. Структура

### ❌ Рисовать копии вместо компонентов

Инстансов компонентов **2,5 %**. Свободных узлов — 3001.

**Цена:** одна правка требует 3001 повторения. Именно поэтому масштабирование
размазалось по всему файлу.

✅ Повторяющийся блок — компонент. Экран собирается из инстансов.

### ❌ Не заводить текстовые стили

Стилей **1,4 %**. Отсюда **129 сочетаний** размера и начертания при требуемых 7–9.

**Цена:** кегли не удерживаются ничем и расходятся при каждой генерации.

✅ Текстовые стили заведены и привязаны. Кегль задаётся стилем, не числом.

### ❌ Ставить цвет числом вместо токена

Заливки привязаны к переменным на 70,7 % — и именно поэтому палитра почти
не поплыла. Обводки: 3 %.

✅ Любой цвет — из переменной. Ноль хардкода.

---

## 4. Цвет

### ❌ Заводить почти-дубли

`#C7F26B` рядом с `#C7F36B`. `#0F120F` рядом с `#101211`. Разница — один пункт
из 255, глазом не отличить.

**Цена:** 64 узла на двух парах, палитра растёт незаметно.

✅ Брать существующий токен. Не «почти такой же».

### ❌ Вводить семантику, не объявив её

Строки клиентов кремовые `#FFF5C7`, мятные `#E5FAE5`, иконка ошибки оранжевая
`#8C610D`. Это рабочий язык статусов — оплачено, не оплачено, нет активности,
ошибка. В токенах его нет.

**Цена:** аудит записывал их в «цвета вне палитры», то есть в мусор.

✅ Новая семантика — сначала токен в Foundations, потом применение.

### ❌ Ставить текст на фон того же цвета

`#101211` на `#101211`, контраст 1,0. И белый `#FFFFFF` на светло-сером
`#EEF0EB` — контраст 1,15 при норме 4,5.

✅ Проверять контраст: 4,5 для текста, 3,0 для крупного и для иконок.

---

## 5. Компоновка

### ❌ Больше одного акцента на экране

**24 экрана из 31** несут больше одного лаймового элемента, до двенадцати
на одном.

**Цена:** акцент перестаёт быть акцентом — глазу не за что зацепиться.

✅ Один акцент на экран. Остальное — вторичный стиль.

### ❌ Не оставлять запаса по ширине

**171 однострочный текст** занимает ≥ 90 % ширины контейнера.

**Цена:** длинное имя клиента или крупная сумма не поместятся, а вариантов
с такими данными в макете нет ни одного.

✅ Проверять на предельных данных: длинное имя, максимальная сумма, длинное
название работы.

### ❌ Выпускать элемент за границу экрана

Кнопка Continue на iPhone 8/SE уходит под нижний край на 9 pt — обрезана.

✅ Проверять на самом маленьком поддерживаемом экране.

### ❌ Цель нажатия меньше 44 × 44

Проверять обе стороны, а не только высоту.

### ❌ Отступы не из шкалы

Шкала 4 / 8 / 12 / 16 / 24 / 32 / 40. Найдено: 8,72 · 4,36 · 10,47 · 7,85.

Отдельно: **1365 зазоров** между вручную расставленными слоями. Отступ должен
задаваться auto-layout, а не положением слоя — иначе он не проверяется и
не держится.

---

## 6. Прототип

### ❌ Отдавать экраны без переходов

У экранов 01–07 онбординга **ноль исходящих переходов**. Связаны только 08–13 —
их сорок переходов видно на канвасе, и по ним кажется, что прототип собран.

**Цена:** онбординг невозможно ни протестировать, ни показать.

✅ Каждый экран потока связан со следующим.

### ❌ Ставить точку входа на доску вместо экрана

Точка входа названа «Onboarding · 01 Welcome», а ведёт на узел размером
**6444 × 1320** — всю доску. Нажатие play показывает не телефон, а всю раскладку
из тринадцати экранов.

✅ Точка входа — конкретный экран.

### ❌ Оставлять недостижимые экраны

Три экрана без входящих переходов: `02.3C`, `02.3E` и обложка.

✅ Экран либо подключён, либо удалён.

---

## 7. Логика

### ❌ Копировать экран и не переименовывать слои

Экран `02.3E — Send failed` собран из экрана успеха. Слои: `Success icon`,
`Success mark`, `Success title`, `Success helper`.

**Цена:** правка по имени слоя попадёт не туда.

### ❌ Ставить «назад» на первом экране

Welcome — первая точка приложения. Кнопка есть, возвращаться некуда,
перехода у неё нет.

### ❌ Называть экран «Populated» и класть один элемент

`02.1B — Overview · Populated` — один документ и 40 % пустоты.

✅ Демо-данные показывают реальный объём.

---

## 8. Границы генерации

### ❌ Перегенерировать дизайн-систему

`00 — Foundations` собрана вручную с первого часа: веб-следов **0,8 %**,
дробных значений **2,8 %**. Это **источник**, а не выход.

Для сравнения: экраны — 48,9 % и 83,1 % в онбординге.

✅ Генерировать только экраны. Foundations читать как источник токенов
и не трогать.

---

## Чек перед отдачей

```
[ ] ширина = ширине мастера, ничего не масштабировалось после
[ ] все значения целые: радиусы, кегли, отступы, обводки
[ ] имена слоёв описывают роль, а не разметку
[ ] цвета из переменных, новых почти-дублей нет
[ ] повторяющиеся блоки — инстансы компонентов
[ ] кегли заданы текстовыми стилями
[ ] один акцент на экран
[ ] контраст 4,5 для текста, цели нажатия 44 × 44
[ ] у каждого экрана потока есть исходящий переход
[ ] точка входа указывает на экран, а не на доску
[ ] после копирования экрана слои переименованы
[ ] Foundations не тронута
```
